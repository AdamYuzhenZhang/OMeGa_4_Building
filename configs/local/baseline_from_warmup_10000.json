{
  "kind": "baseline",
  "description": "10000-step clean baseline branch resumed from the shared warmup checkpoint, with denser official OMeGa mesh subdivision.",
  "extends": "baseline_debug_6500.json",
  "args": {
    "result_dir": "${PACKAGE_ROOT}/omega/model_baseline_from_warmup_10000",
    "resume_from_checkpoint": "${PACKAGE_ROOT}/omega/model_warmup_2999/ckpts/ckpt_2999_rank0.pt",
    "max_steps": 10000,
    "save_steps": [
      10000
    ],
    "eval_steps": [
      5000,
      7500,
      10000
    ],
    "mesh_loss_start_iter": 1000,
    "mono_normal_loss_start_iter": 3000,
    "dist_start_iter": 3000,
    "mesh_refine_start_iter": 3000,
    "refine_stop_iter": 10000,
    "mesh_split_topk": 0.05,
    "split_every": 250,
    "mesh_preview_max_width": 0
  }
}
