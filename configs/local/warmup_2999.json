{
  "kind": "warmup",
  "description": "Shared OMeGa warmup through global step 2999. OMeGa mesh smooth/normal losses start at 1000; extension losses start only after branching.",
  "extends": "baseline_debug_6500.json",
  "args": {
    "result_dir": "${PACKAGE_ROOT}/omega/model_warmup_2999",
    "max_steps": 3000,
    "save_steps": [3000],
    "eval_steps": [3000],
    "mesh_loss_start_iter": 1000,
    "mono_normal_loss_start_iter": 3000,
    "dist_start_iter": 3000,
    "mesh_refine_start_iter": 3000,
    "refine_stop_iter": 3000,
    "remove_start_iter": 5000
  }
}
