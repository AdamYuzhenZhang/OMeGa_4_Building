{
  "kind": "baseline",
  "description": "20000-step clean OMeGa baseline branch resumed from the shared warmup checkpoint. Uses denser mesh subdivision than the official default: mesh_split_topk=0.05 and split_every=250.",
  "extends": "baseline_debug_6500.json",
  "args": {
    "result_dir": "${PACKAGE_ROOT}/omega/model_baseline_from_warmup_20000",
    "resume_from_checkpoint": "${PACKAGE_ROOT}/omega/model_warmup_2999/ckpts/ckpt_2999_rank0.pt",
    "max_steps": 20000,
    "save_steps": [
      20000
    ],
    "eval_steps": [
      5000,
      10000,
      15000,
      20000
    ],
    "mesh_loss_start_iter": 1000,
    "mono_normal_loss_start_iter": 3000,
    "dist_start_iter": 3000,
    "mesh_refine_start_iter": 3000,
    "refine_stop_iter": 15000,
    "mesh_split_topk": 0.05,
    "split_every": 250,
    "mesh_preview_max_width": 0,
    "mesh_preview_contact_sheet_images": 64
  }
}
