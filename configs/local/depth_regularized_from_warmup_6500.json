{
  "kind": "extension",
  "description": "6500-step depth-regularized branch resumed from the shared warmup checkpoint.",
  "extends": "depth_regularized_6500.json",
  "args": {
    "result_dir": "${PACKAGE_ROOT}/omega/model_depth_regularized_from_warmup_6500",
    "resume_from_checkpoint": "${PACKAGE_ROOT}/omega/model_warmup_2999/ckpts/ckpt_2999_rank0.pt",
    "mesh_loss_start_iter": 1000,
    "mono_normal_loss_start_iter": 3000,
    "dist_start_iter": 3000,
    "mesh_refine_start_iter": 3000
  }
}
